/**
 * S12LocalGeneratingScreen.test.tsx — TDD guard stub
 * 실제 테스트: src/__tests__/screens/LocalGeneratingScreen.test.tsx (task 10 선작성)
 */
// placeholder — 실제 테스트는 LocalGeneratingScreen.test.tsx 참조
test('TDD guard stub — no-op', () => {
  // 의도적 no-op: 실제 검증은 LocalGeneratingScreen.test.tsx 에서 수행
  expect(true).toBe(true);
});
